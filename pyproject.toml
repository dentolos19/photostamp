[project]
name = "photostamp"
version = "1.0.0"
requires-python = ">=3.13"
dependencies = ["colorama", "hachoir", "pillow"]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff",
]

[tool.ruff]
line-length = 120
