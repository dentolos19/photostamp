[project]
name = "photostamp"
version = "1.0.0"
requires-python = ">=3.13"
dependencies = ["colorama", "hachoir", "piexif>=1.1.3", "pillow"]

[dependency-groups]
dev = ["ruff", "ty"]

[tool.ruff]
line-length = 120
